---
sidebar_position: 4
---

# Label

![Label](/img/components/label.png)

## Usage

```lua title="Client/Index.lua"
local label = EGUI.Label(10, 10, 100, 100, "Label Test")
```

## Constructor Parameters

| **Type** | **Name** | **Default** |
| :--- | :--- | :--- |
| `number` | **`posX`** |  |
| `number` | **`posY`** |  |
| `number` | **`width`** |  |
| `number` | **`height`** |  |
| `string` | **`text`** |  |
| `table` | **`parent`** | `nil` |

## Functions

import BaseElementFunctions from '@site/docs/components/_base-element-functions.mdx';

<details><summary>Inherited Element Functions</summary><BaseElementFunctions /></details>

| **Returns** | **Name** | **Description** |
| :--- | :--- | :--- |
| [`Label`](label) | [**`SetText`**](label#settext) | Sets the text |
| `string` | [**`GetText`**](label#gettext) | Gets the text |
| [`Label`](label) | [**`SetFontSize`**](label#setfontsize) | Sets the font size |
| `number` | [**`GetFontSize`**](label#getfontsize) | Gets the font size |
| [`Label`](label) | [**`SetLineHeight`**](label#setlineheight) | Sets the line height |
| `number` | [**`GetLineHeight`**](label#getlineheight) | Gets the line height |

## SetText

Sets the text of the label

> Returns the [`Label`](label) (self)

```lua
label:SetText("Text")
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| `string` | **`text`** |  | The new text |

## GetText

Gets the text of the label

> Returns a `string`

```lua
local text = label:GetText()
```

## SetFontSize

Sets the font size of the label

> Returns the [`Label`](label) (self)

```lua
label:SetFontSize(20)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| `string` | **`size`** |  | The new size in pixels |

## GetFontSize

Gets the font size of the label

> Returns a `number`

```lua
local size = label:GetFontSize()
```
## SetLineHeight

Sets the line height of the label

> Returns the [`Label`](label) (self)

```lua
label:SetLineHeight(20)
```

| Type | Parameter | Default Value | Description |
| :--- | :--- | :--- | :--- |
| `string` | **`size`** |  | The new line height in pixels |

## GetLineHeight

Gets the line height of the label

> Returns a `number`

```lua
local lineHeight = label:GetLineHeight()
```
